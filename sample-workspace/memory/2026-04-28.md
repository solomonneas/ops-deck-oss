## 09:00 - Sample journal entry
This is what the Journal page renders. Replace this directory with your
own OpenClaw workspace via the OPENCLAW_WORKSPACE env var.

## 14:30 - Another section
Each `## HH:MM - Title` block becomes a section in the rendered entry.
The auto-tagger picks up keywords (push, fix, research, deploy) and
assigns tags.
