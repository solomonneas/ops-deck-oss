---
topic: Sample memory card
category: docs
tags: [sample, getting-started]
---
This is what the Memory page renders. Cards live in `memory/cards/*.md`
and use YAML frontmatter for the topic/category/tags.

The card body is plain markdown; the API exposes it as the `body` field
on the detail endpoint.
